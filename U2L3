A = ["zebra", "kangaroo", "cat", "human", "aardvark"]

def swap(A, i, j):
  temp = A[i]
  A[i] = A[j]
  A[j] = temp


for i in range(0, len(A) - 1):
  for j in range(i + 1, len(A)):
    if A[i] > A[j]:
      swap(A, i, j)

print(A)
